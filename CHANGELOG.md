# 质检优化助手 · 变更记录（CHANGELOG）

## v5.3.3（2026-09-22）修改方式逐条采纳

### 变更内容

**交互变更**：卡片「修改方式」右上角的「📋 复制」「✏️ 编辑」两个按钮移除，替换为单个「✅ 采纳」按钮。

**采纳逻辑**（content.js）：

- 点击「采纳」→ 该条修改方式按其「原文定位」在规则原文中定位，把「修改后」对应段落替换为本条修改方式文本，替换内容为**绿色高亮**（修改后列高亮本就是绿色系，沿用）；
- 原文列同步高亮被替换掉的原文区间（蓝色，与原合并行为一致）；
- 再次点击（按钮变为「↩ 取消采纳」实心绿底）→ 取消采纳，该条恢复为原文，绿色高亮消失；
- 新增 `cmpAdopted` 集合（对象引用 = lastProblems 条目），`cmpBuildMerged()` 只替换集合内条目——**原文对比不再自动全量替换**，未采纳时「修改后」= 原文；
- 定位预检与合并同口径（score ≥ 0.5）：无法可靠定位的条目点击采纳时提示「未能在原文中定位该条」，不强行替换；
- 原文未获取时点击采纳：自动展开对比区并带参获取原文，提示稍后重试；
- 手动编辑保护：「修改后」被手动编辑过时，采纳/取消先 confirm 确认，避免重算悄悄覆盖手工内容；
- 采纳状态随生命周期清理：新一轮对话、重新格式化、重载格式、面板重建时 `cmpAdopted.clear()`（条目对象全新，旧引用失效）。

**删除**：`openFixEditor()`（修改方式就地编辑）与 `cmpSyncFixChange()`（编辑保存后同步）——编辑入口已移除；panel.css 中 `qc-fix-edit-*` 系列死样式一并清理。

### 影响

- 修改后内容由用户逐条控制，避免「自动替换全部」把低分/不想要的条目也拼进去；
- 埋点：`修改方式采纳`（action=adopt/unadopt，含 adopted/total 计数），替代原 `优化结果复制`/`优化规则编辑`。

## v5.3.2（2026-09-22）输出去重

### 问题现象

Agent 在同一条回复里会把「优化结果」输出多遍，插件提取后原样拼进结果，表现为：

- 同一批「第 N 条」问题条目在卡片里出现两次（草稿版措辞与最终版略有差异，原有精确指纹去重拦不住）；
- 草稿里的思考过程残留（`Let me write out the 4 issues clearly…` 等英文过程语句）混进正文；
- 结果文本体积膨胀（实测一份三重复样例 4317 字，去重后 1703 字）。

典型样例结构：`带思考残留的草稿块` + `完整版` + `近重复微调版`，共 3 个「## 优化结果」块。

### 根因

整页提取走 `firstMarkerIndex`（从**第一个**「## 优化结果」标记切起，用于合并多轮结果——该语义保留不变），
Agent 单条回复内的重复块会全部被拼进来；而 `structureProblems` 的条目去重只做
`locate|desc|fix` 精确指纹比对，措辞微调的复述条目指纹不同，无法合并。

### 修改内容（均在 content.js）

#### 1. 块级去重 `dedupeResultBlocks()`（content.js:242）

按「## 优化结果」标记把文本切块后，对相邻块两两判定，丢弃「重复/草稿」块：

| 规则 | 条件 | 动作 |
|---|---|---|
| ① 草稿块 | 含 ≥2 行思考残留（`THINK_LINE_RE` 命中） | 丢弃，保留后面更完整的块 |
| ② 近重复块 | 与后面的块 bigram-Dice 相似度 ≥ 0.85（归一化：去标记行/小写/去空白） | 丢弃，**只保留最后一个（最终版）** |
| ③ 草稿片段 | 有信息行（≥6 字符、非标记/思考行）≥85% 被后面的块包含 | 丢弃 |

**防误伤守卫**：双方头部都有「规则 ID」且不同 → 视为不同轮次的不同结果，一律保留，
不影响「合并所有轮次」的原有提取语义；全部块被误判时兜底返回原文。

#### 2. 行级剥除 `stripThinkLines()`（content.js:203）

剔除 `Let me…` / `Let's…` / `Draft content…` / `让我…` / `我需要…` 等过程性整行
（模式表：`THINK_LINE_RE`，content.js:200），只删整行不碰正文。单块结果也会走这一步。

#### 3. 条目级近重复去重（`structureProblems` 内，content.js:461）

新增 `seenNum` 条号 → 字段指纹映射：同一「第 N 条」条号且内容相似度（`qcTextSim`）≥ 0.8
→ 视为同一条的复述微调版，保留首份；内容实质不同的同条号条目（相似度低）不受影响，仍全部保留。

#### 4. 接入点（两条提取路径全覆盖）

- 整页提取 `extractResult()`（content.js:300）：`sliceResultText(dedupeResultBlocks(text.slice(idx)))`
  ——先块级去重再做尾部噪音截断，避免草稿块里的噪音词提前触发截断标记；
- 对话回复退化解析路径（content.js:2082）：同样先 `dedupeResultBlocks` 再 `sliceResultText(…, 60)`。

#### 5. 辅助函数

- `qcTextSim(a, b)`（content.js:212）：字符 bigram Dice 相似度，任一方 <20 字符返回 0（过短不参与判定，防误伤短条目）；
- `qcSubsetRatio(aLines, bNorm)`（content.js:233）：行包含比例，草稿片段检测用。

### 其他变更

- manifest.json：版本 5.3.1 → **5.3.2**，description 追加变更说明。

### 行为不变项（明确不受影响）

- 多轮对话中**不同规则 ID** 的结果块全部保留（合并所有轮次语义）；
- 旧气泡残留 + 新流式输出中途的瞬时双块：新块写完后下一轮自愈去重；
- 尾部噪音截断、占位符/长度校验、高亮定位逻辑均未改动。

### 验证

验证脚本 `test-dedupe.js`（workspace/qc-plugin-work/）从 content.js 切片真实函数跑管线，15/15 通过：

- 真实三重复样例：3 块 + 思考残留 → 1 块、4 条问题各出现一次、体积 4317 → 1703 字；
- 不同规则 ID 双块 → 全保留；同规则 ID 近重复双块 → 只留最终版；
- 单块思考残留行剥除、正文无损；条目级同条号近重复 → 1 条、实质不同 → 2 条都保留。

### 交付

- 插件包：`qc-plugin-fixed-v2-dedup.zip`（桌面，16 个文件，含本变更记录）；
- 安装：`chrome://extensions` 重新加载插件目录即可生效。
